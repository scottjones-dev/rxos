# Tooling

Repository-wide scripts are currently declared in the root `package.json`,
`turbo.json`, Cargo workspace and top-level CMake project. Add developer-only
generators and validation utilities here.
